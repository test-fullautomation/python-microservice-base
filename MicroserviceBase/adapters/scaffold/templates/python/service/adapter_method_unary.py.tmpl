
    async def @@{method}(self, request, context):
        """
Handle a unary @@{method} RPC.

**Arguments:**

* ``request``

  / *Condition*: required / *Type*: @@{method}Request /

  @@{request_doc}

* ``context``

  / *Condition*: required / *Type*: grpc.aio.ServicerContext /

  Per-call context (deadline, metadata, abort, …).

**Returns:**

* ``response``

  / *Type*: @@{method}Response /

  Proto response message with field ``result``.
        """
@@{param_reads}
        result = await self._domain.@@{snake_method}(@@{args})
        return @@{sn}_pb2.@@{method}Response(result=str(result))
