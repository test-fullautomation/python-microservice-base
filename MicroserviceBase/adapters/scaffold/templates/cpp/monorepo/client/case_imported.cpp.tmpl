            case @@{idx}: {
                grpc::ClientContext ctx;
                @@{inT} req;   // TODO: fill fields — set_<field>(...)
                @@{outT} resp;
                auto st = stub.@@{method}(&ctx, req, &resp);
                if (st.ok()) std::cout << "  @@{method}: OK (fill in response print)\n";
                else         std::cerr << "  @@{method} FAILED: " << st.error_message() << "\n";
                break;
            }